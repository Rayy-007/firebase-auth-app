import { Link } from "react-router-dom";

const Home = () => {
  return (
    <div className="container">
      <Link to="/register">Sign Up With Email</Link>
    </div>
  );
};

export default Home;
